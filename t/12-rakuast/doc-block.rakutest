use v6.e.PREVIEW;
use Test;

plan 22;

my $ast;
my $deparsed;
my $raku;
sub ast(RakuAST::Node:D $node --> Nil) {
    $ast      := $node;
    $deparsed := $node.DEPARSE;
    $raku     := 'use experimental :rakuast; ' ~ $node.raku;
    diag $deparsed.chomp;
}

subtest 'simple documentation' => {
    #   =begin doc␤␤  This is documentation␤␤  =end doc␤␤
    ast RakuAST::Doc::Block.new(
      margin      => '  ',
      type        => 'doc',
      paragraphs  => (
        "This is documentation\n\n",
      )
    );
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
  =begin doc

  This is documentation

  =end doc

CODE
    }
}

subtest 'simple documentation abbreviated' => {
    # =doc This is documentation␤␤
    ast RakuAST::Doc::Block.new(
      margin      => '  ',
      type        => 'doc',
      abbreviated => True,
      paragraphs  => (
        "This is documentation\n",
      )
    );
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
  =doc This is documentation
CODE
    }
}

subtest 'simple documentation with markup' => {
    #   =begin doc␤␤  This is B<doc>umentation␤␤  =end doc␤␤
    ast RakuAST::Doc::Block.new(
      margin      => '  ',
      type        => 'doc',
      paragraphs  => (
        RakuAST::Doc::Paragraph.new(
          "This is ",
          RakuAST::Doc::Markup.new(
            letter => "B",
            atoms  => (
              "doc",
            )
          ),
          "umentation\n\n",
        ),
      )
    );
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
  =begin doc

  This is B<doc>umentation

  =end doc

CODE
    }
}

subtest 'simple documentation in a statementlist (1)' => {
    # 42␤  =begin doc␤␤  This is documentation␤␤  =end doc␤␤
    ast RakuAST::StatementList.new(
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      ),
      RakuAST::Doc::Block.new(
        margin      => '  ',
        type        => 'doc',
        paragraphs  => (
          "This is documentation\n\n",
        )
      )
    );
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
42
  =begin doc

  This is documentation

  =end doc

CODE
    }
}

subtest 'simple documentation in a statementlist (2)' => {
    #   =begin doc␤␤  This is documentation␤␤=  end doc␤␤42␤
    ast RakuAST::StatementList.new(
      RakuAST::Doc::Block.new(
        margin      => '  ',
        type        => 'doc',
        paragraphs  => (
          "This is documentation\n\n",
        )
      ),
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      )
    );
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
  =begin doc

  This is documentation

  =end doc

42
CODE
    }
}

subtest 'abbreviated documentation in a statementlist (1)' => {
    # 42␤  =head1 This is documentation␤
    ast RakuAST::StatementList.new(
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      ),
      RakuAST::Doc::Block.new(
        margin      => '  ',
        type        => 'head',
        level       => 1,
        abbreviated => True,
        paragraphs  => (
          "This is documentation\n",
        )
      )
    );
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
42
  =head1 This is documentation
CODE
    }
}

subtest 'abbreviated documentation in a statementlist (2)' => {
    #   =head1 This is documentation␤42␤
    ast RakuAST::StatementList.new(
      RakuAST::Doc::Block.new(
        margin      => '  ',
        type        => 'head',
        level       => 1,
        abbreviated => True,
        config      => { :numbered },
        paragraphs  => (
          "This is documentation\n\n",
        )
      ),
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      )
    );

    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
  =head1 # This is documentation

42
CODE
    }
}

subtest 'alias directive in a statement list' => {
    #   =alias FOO bar␤  = 137␤42␤
    ast RakuAST::StatementList.new(
      RakuAST::Doc::Block.new(
        margin      => '  ',
        type        => 'alias',
        directive   => True,
        paragraphs  => (
          "FOO",
          "bar\n137",
        )
      ),
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      )
    );

    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
  =alias FOO bar
  =          137
42
CODE
    }
}

for <row column> -> $name {
    subtest "$name directive in a statement list" => {
        #   =$type :align<left middle>␤42␤
        ast RakuAST::StatementList.new(
          RakuAST::Doc::Block.new(
            margin     => "  ",
            type       => "table",
            paragraphs => (
              RakuAST::Doc::Block.new(
                margin     => "  ",
                type      => $name,
                directive => True,
                config    => ${:align(RakuAST::QuotedString.new(
                  processors => <words val>,
                  segments   => (
                    RakuAST::StrLiteral.new("left middle"),
                  )
                ))},
              )
            )
          ),
          RakuAST::Statement::Expression.new(
            expression => RakuAST::IntLiteral.new(42)
          )
        );

# Complains about an unexplained RakuAST::Var::Lexical::Setting not
# having been resolved, so skip for now
#        is-deeply $ast.EVAL, 42, 'do we get the final result';
        for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
            is-deeply $it, qq:to/CODE/, "$type: deparse";
      =begin table

      =$name :align<left middle>
      =end table

    42
    CODE
        }
    }
}

subtest 'abbreviated defn in a statement list' => {
    #   =defn Foo Bar␤  baz␤42␤
    ast RakuAST::StatementList.new(
      RakuAST::Doc::Block.new(
        margin      => '  ',
        type        => 'defn',
        abbreviated => True,
        paragraphs  => (
          "Foo Bar",
          "baz\n\n",
        )
      ),
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      )
    );

    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
  =defn Foo Bar
  baz

42
CODE
    }
}

subtest 'pod block inside a class body' => {
    # my class A {␤    =begin pod␤␤    hi␤    =end pod␤␤    method x {␤        42␤    }␤}␤A.x
    ast RakuAST::StatementList.new(
      RakuAST::Statement::Expression.new(
        expression => RakuAST::Class.new(
          scope => 'my',
          name  => RakuAST::Name.from-identifier('A'),
          body  => RakuAST::Block.new(
            body => RakuAST::Blockoid.new(
              RakuAST::StatementList.new(
                RakuAST::Doc::Block.new(
                  margin     => '    ',
                  type       => 'pod',
                  paragraphs => ("hi\n",)
                ),
                RakuAST::Statement::Expression.new(
                  expression => RakuAST::Method.new(
                    name => RakuAST::Name.from-identifier('x'),
                    body => RakuAST::Blockoid.new(
                      RakuAST::StatementList.new(
                        RakuAST::Statement::Expression.new(
                          expression => RakuAST::IntLiteral.new(42)
                        )
                      )
                    )
                  )
                )
              )
            )
          )
        )
      ),
      RakuAST::Statement::Expression.new(
        expression => RakuAST::ApplyPostfix.new(
          operand => RakuAST::Type::Simple.new(
            RakuAST::Name.from-identifier('A')
          ),
          postfix => RakuAST::Call::Method.new(
            name => RakuAST::Name.from-identifier('x')
          )
        )
      )
    );

    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
my class A {
    =begin pod

    hi
    =end pod

    method x {
        42
    }
}
A.x
CODE
    }
    for 'AST', $ast, 'Str', $deparsed, 'Raku', EVAL($raku) -> $type, $it {
        is-deeply EVAL($it), 42, "$type: the method after the pod block runs";
    }
}


subtest 'The .raku of a table row rebuilds a row that stringifies' => {
    # =begin table
    # a | b
    # =end table
    ast RakuAST::Doc::Block.new(
      type       => 'table',
      paragraphs => (
        RakuAST::Doc::LegacyRow.new(
          column-dividers => '|',
          column-offsets  => (4,),
          cells           => ('a', 'b')
        ),
      )
    );
    is-deeply $deparsed, "=begin table\n\na | b\n\n=end table\n\n", 'deparse';

    is-deeply EVAL($raku).DEPARSE, $deparsed, 'Raku: deparses the same';
}


subtest 'An abbreviated table is followed by the blank line that ends it' => {
    # =table
    # a  1
    # b  2
    #
    # 42
    ast RakuAST::StatementList.new(
      RakuAST::Doc::Block.new(
        type        => 'table',
        abbreviated => True,
        paragraphs  => (
          RakuAST::Doc::LegacyRow.new(
            column-dividers => '',
            column-offsets  => (0, 3),
            cells           => ('a', '1')
          ),
          RakuAST::Doc::LegacyRow.new(
            column-dividers => '',
            column-offsets  => (0, 3),
            cells           => ('b', '2')
          ),
        )
      ),
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      )
    );
    is-deeply $deparsed, "=table\na  1\nb  2\n\n42\n", 'deparse';

    is-deeply EVAL($raku).DEPARSE, $deparsed, 'Raku: deparses the same';
    is-deeply EVAL($deparsed), 42, 'Str: the statement after the table runs';
}


subtest 'A visual table that starts with a divider' => {
    # =table
    # +-----+----+---+
    # |   a | b  | c |
    # +-----+----+---+
    # | foo | 52 | Y |
    # +-----+----+---+
    #
    # 42
    ast Q:to/SOURCE/.AST;
    =table
    +-----+----+---+
    |   a | b  | c |
    +-----+----+---+
    | foo | 52 | Y |
    +-----+----+---+

    42
    SOURCE
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, Q:to/CODE/, "$type: deparse";
=table
+-----+----+---+
|   a | b  | c |
+-----+----+---+
| foo | 52 | Y |
+-----+----+---+

42
CODE
    }

    # =numtable
    # +---+---+
    # | a | b |
    # +---+---+
    # | 1 | 2 |
    # +---+---+
    #
    # 42
    ast Q:to/SOURCE/.AST;
    =numtable
    +---+---+
    | a | b |
    +---+---+
    | 1 | 2 |
    +---+---+

    42
    SOURCE
    is-deeply $ast.EVAL, 42, 'do we get the final result of the numbered table';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, Q:to/CODE/, "$type: deparse of the numbered table";
=numtable
+---+---+
| a | b |
+---+---+
| 1 | 2 |
+---+---+

42
CODE
    }
}


subtest 'An abbreviated block without text ends with a blank line' => {
    # =twigils
    #
    # 42
    ast RakuAST::StatementList.new(
      RakuAST::Doc::Block.new(
        type        => 'twigils',
        abbreviated => True,
        paragraphs  => ("\n",)
      ),
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      )
    );
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, Q:to/CODE/, "$type: deparse";
=twigils

42
CODE
    }
    is-deeply $deparsed.EVAL, 42, 'Str: the code after the blocks runs';
}


subtest 'An implicit code block ends with the blank line that ends it' => {
    # =begin pod
    #
    # para
    #
    #     code
    #
    # after
    #
    # =end pod
    #
    # 42
    ast Q:to/SOURCE/.AST;
    =begin pod

    para

        code

    after

    =end pod

    42
    SOURCE
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, Q:to/CODE/, "$type: deparse";
=begin pod

para

    code

after

=end pod

42
CODE
    }
    is-deeply $deparsed.AST.DEPARSE, $deparsed,
      'the deparse parses back to itself';
}


subtest 'An escaped divider in a table cell is not a column divider' => {
    # =table
    # a  | b
    # ===+====
    # \| | .\+
    #
    # 42
    ast Q:to/SOURCE/.AST;
    =table
    a  | b
    ===+====
    \| | .\+

    42
    SOURCE
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, Q:to/CODE/, "$type: deparse";
=table
a  | b
===+====
\| | .\+

42
CODE
    }
    is-deeply $deparsed.AST.DEPARSE, $deparsed,
      'the deparse parses back to itself';

    # =table
    # a+b | c
    # ====+==
    # d   | e
    #
    # 42
    ast Q:to/SOURCE/.AST;
    =table
    a+b | c
    ====+==
    d   | e

    42
    SOURCE
    is-deeply $ast.EVAL, 42, 'a row with a + in a cell compiles';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, Q:to/CODE/, "$type: deparse of the row with a + in a cell";
=table
a+b | c
====+==
d   | e

42
CODE
    }

    # =begin table
    # r0c0  |  r0c1
    #  r1c0 |r0c1
    # =end table
    #
    # 42
    ast Q:to/SOURCE/.AST;
    =begin table
    r0c0  |  r0c1
     r1c0 |r0c1
    =end table

    42
    SOURCE
    is-deeply $ast.EVAL, 42, 'a row whose | is not a column divider compiles';
    is-deeply $deparsed.AST.DEPARSE, $deparsed,
      'the deparse of that row parses back to itself';
}


subtest 'A row that leaves its last column empty ends without a gap' => {
    # =begin table
    #
    # a    b    c
    #
    # d    e
    # f    g    h
    #
    # =end table
    #
    # 42
    ast Q:to/SOURCE/.AST;
    =begin table
    a    b    c

    d    e
    f    g    h
    =end table

    42
    SOURCE
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, Q:to/CODE/, "$type: deparse";
=begin table

a    b    c

d    e
f    g    h

=end table

42
CODE
    }
    is-deeply $deparsed.AST.DEPARSE, $deparsed,
      'the deparse parses back to itself';
}


subtest 'An abbreviated block whose text is only blank lines' => {
    # =back␤␤␤␤
    ast RakuAST::Doc::Block.new(
      type        => 'back',
      abbreviated => True,
      paragraphs  => ("\n\n\n",)
    );
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, "=back\n\n", "$type: deparse";
    }
    is-deeply $deparsed.AST.DEPARSE, $deparsed,
      'the deparse parses back to itself';
}

subtest 'An implicit code block in an indented block keeps its indent' => {
    #   =begin pod
    #
    #   para
    #
    #       code
    #
    #   =end pod
    #
    # 42
    ast Q:to/SOURCE/.AST;
      =begin pod

      para

          code

      =end pod

      42
    SOURCE
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, Q:to/CODE/, "$type: deparse";
  =begin pod

  para

      code

  =end pod

42
CODE
    }
    is-deeply $deparsed.AST.DEPARSE, $deparsed,
      'the deparse parses back to itself';
}

subtest 'The configuration of a block keeps the order it was written in' => {
    # =begin pod :kind<Language> :subkind<Language> :category<basic>
    #
    # text
    # =end pod
    #
    # 42
    sub words(str $text) {
        RakuAST::QuotedString.new(
          processors => <words val>,
          segments   => (RakuAST::StrLiteral.new($text),)
        )
    }
    ast RakuAST::StatementList.new(
      RakuAST::Doc::Block.new(
        type       => 'pod',
        config     => (
          :kind(words("Language")),
          :subkind(words("Language")),
          :category(words("basic")),
        ),
        paragraphs => (
          "text\n",
        )
      ),
      RakuAST::Statement::Expression.new(
        expression => RakuAST::IntLiteral.new(42)
      )
    );
    is-deeply $ast.EVAL, 42, 'do we get the final result';
    for 'Str', $deparsed, 'Raku', $raku.EVAL.DEPARSE -> $type, $it {
        is-deeply $it, q:to/CODE/, "$type: deparse";
=begin pod :kind<Language> :subkind<Language> :category<basic>

text
=end pod

42
CODE
    }
    is-deeply $ast.statements.head.config-keys, <kind subkind category>,
      'the keys are in the order given';
    is-deeply $ast.statements.head.config.keys.sort, <category kind subkind>,
      'the config is still a map';
    is-deeply $deparsed.AST.DEPARSE, $deparsed,
      'the deparse parses back to itself';

    # a map has no order, so its keys are written sorted, numbered first
    my $mapped := RakuAST::Doc::Block.new(
      type       => 'item',
      for        => True,
      config     => {
        :allow(words("B")),
        :numbered(RakuAST::Term::True.new),
        :nested(RakuAST::Term::True.new),
      },
      paragraphs => ("text\n",)
    );
    is-deeply $mapped.config-keys, <numbered allow nested>,
      'map: numbered first, then sorted';
    is-deeply $mapped.DEPARSE, qq:to/CODE/, 'map: deparse';
=for item :numbered :allow<B> :nested
text
CODE
    is-deeply EVAL('use experimental :rakuast; ' ~ $mapped.raku).DEPARSE,
      $mapped.DEPARSE, 'map: the .raku keeps the order';
    lives-ok { EVAL RakuAST::StatementList.new($mapped) },
      'map: the block compiles';

    # the last value of a repeated key wins in its first place
    my $twice := RakuAST::Doc::Block.new(
      type       => 'item',
      for        => True,
      config     => (:allow(words("B")), :nested(RakuAST::Term::True.new), :allow(words("C"))),
      paragraphs => ("text\n",)
    );
    is-deeply $twice.DEPARSE, qq:to/CODE/, 'list: a repeated key keeps its place';
=for item :allow<C> :nested
text
CODE
    throws-like { RakuAST::Doc::Block.new(type => 'item', config => (1, 2)) },
      Exception, message => /'map or a list of pairs'/,
      'a config that is not pairs is refused';

    # a parsed table keeps its order through the header row fixup
    my $table := Q:to/SOURCE/.AST;
    =begin table :caption<Odd> :allow<B>
    a | b
    =====
    1 | 2
    =end table
    SOURCE
    is-deeply $table.statements.head.config-keys, <caption allow header-row>,
      'parsed table: the keys written come first';
    is-deeply EVAL('use experimental :rakuast; ' ~ $table.raku).DEPARSE,
      $table.DEPARSE, 'parsed table: the .raku keeps the order';
    lives-ok { EVAL $table }, 'parsed table: the block compiles';
    is-deeply $table.DEPARSE, Q:to/CODE/, 'parsed table: deparse';
=begin table :caption<Odd> :allow<B>

a | b
=====
1 | 2

=end table

CODE
}

# vim: expandtab shiftwidth=4
