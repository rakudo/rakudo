# Copyright (C) 2015 The Perl Foundation

PERL5   = @perl@
MKPATH  = $(PERL5) -MExtUtils::Command -e mkpath
CHMOD   = $(PERL5) -MExtUtils::Command -e chmod
CP      = $(PERL5) -MExtUtils::Command -e cp
RM_F    = $(PERL5) -MExtUtils::Command -e rm_f
RM_RF   = $(PERL5) -MExtUtils::Command -e rm_rf
TEST_F  = $(PERL5) -MExtUtils::Command -e test_f
SHELL   = @shell@

SYSROOT= @sysroot@
SDKROOT= @sdkroot@
PREFIX = @prefix@
PERL6_LANG_DIR = $(PREFIX)/share/perl6

BOOTSTRAP_SOURCES = \
  src/Perl6/Metamodel/BOOTSTRAP.nqp \
  src/Perl6/Metamodel/EXPORTHOW.nqp \

COMMON_BOOTSTRAP_SOURCES = \
	@common_bootstrap_sources@

R_SETTING_SRC = src/RESTRICTED.setting
